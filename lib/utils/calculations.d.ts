/**
 * Utility to calculate percentage-based dimensions
 */
export declare const percentageCalculation: (max: number, val: number) => number;
/**
 * Utility to calculate font size based on screen dimensions and aspect ratio
 */
export declare const fontCalculation: (height: number, width: number, val: number) => number;
